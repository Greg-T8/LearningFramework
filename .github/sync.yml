# Repos you want to receive files from LearningSessions

group:
  - files:
      - source: .markdownlint.yml
        dest: .markdownlint.yml

      - source: templates/issues/
        dest: .github/ISSUE_TEMPLATE/
        deleteOrphaned: true

      - source: templates/workflows/automerge.yml
        dest: .github/workflows/automerge.yml
        deleteOrphaned: true

      - source: templates/labs/
        dest: templates/labs/
        deleteOrphaned: true

      - source: templates/pull_requests/
        dest: .github/
        deleteOrphaned: true

      - source: scripts/
        dest: scripts/
        deleteOrphaned: true

    repos: |
      Greg-T8/LearningTopic

  # - files: # Example of another group
  #     - source: templates/pull_request_template.md
  #       dest: .github/pull_request_template.md
  #   repos: |
